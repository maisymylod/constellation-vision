# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Build venvs (created for training, deleted before commit; never committed)
.venv/
.venv-*/
venv/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Generated datasets (regenerate with `make data`)
data/*.npz

# Trained weights are regenerated by `make train`; ONNX is committed for the
# enrichment demo, the torch checkpoint is not.
src/constellation_vision/model/weights/segmenter.pt
